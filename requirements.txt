# ── Core pipeline (karaoke_timing.py + karaoke_assembler.py) ──────────────────
Pillow>=10.0
openai-whisper
soundfile>=0.12
librosa>=0.10

# ── Audio separation (karaoke_separator.py) ───────────────────────────────────
# audio-separator pulls diffq-fixed, which fails to build on Windows (missing
# bitpack.pyx in the Cython source tarball). Install its real deps manually,
# then install audio-separator with --no-deps to skip the broken one.
#
# Step 1 — real dependencies (audio-separator 0.44.2):
#   pip install beartype einops julius ml_collections numpy \
#       onnx2torch pydub pyyaml requests resampy \
#       rotary-embedding-torch samplerate scipy six soundfile torch tqdm
#
# Step 2 — install the package itself without pulling diffq-fixed:
#   pip install audio-separator --no-deps

# Optional: MIDI transcription from stems using `basic-pitch`.
# Note: as of this project snapshot, `basic-pitch` may require an older
# `numpy` and a Python version < 3.14 to build successfully. If you need
# to run `stem2Midi.py`, create a separate virtualenv using Python 3.10-3.12
# and then install:
#
#   python -m venv .venv_bp
#   .venv_bp\Scripts\activate
#   pip install --upgrade pip
#   pip install basic-pitch
#
# We don't pin `basic-pitch` here because it is optional and can conflict
# with the main project's `numpy` version. Add it manually when needed.
